import 'package:flutter/material.dart';

class DashboardBottom extends StatelessWidget {
  const DashboardBottom({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(child: Text("DashboardBottom"));
  }
}
